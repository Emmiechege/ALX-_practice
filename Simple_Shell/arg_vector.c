#include <stdio.h>
/**
 * main - prints all the  arguments without using argc
 * Return: 0
 */
int main(int argc, char **argv)
{
	int args = 0;

	if
	{
		**argv != NULL;
		printf("argv[args]", args);

	}
	return (0);
}
